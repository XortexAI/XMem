---
name: memory-init
description: Check or initialize XMem memory setup for Cursor.
---

1. Call `xmem_status`.
2. If `XMEM_API_KEY` is missing, ask the user to set it in their shell or secret manager.
3. If configured, explain that XMem search/add tools are ready.
